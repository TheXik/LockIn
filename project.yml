name: LockIn
options:
  bundleIdPrefix: com.lukashellesch.lockin
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "15.0"

packages:
  Supabase:
    url: https://github.com/supabase/supabase-swift.git
    from: "2.0.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"
    TARGETED_DEVICE_FAMILY: "1"  # iPhone only
    DEVELOPMENT_TEAM: "Y2JC2VK9VP"

schemes:
  LockIn:
    build:
      targets:
        LockIn: all
    run:
      config: Debug
      executable: LockIn
    archive:
      config: Release

targets:

  # ─── Main App ───────────────────────────────────────────
  LockIn:
    type: application
    platform: iOS
    sources:
      - path: LockIn
        excludes:
          - "**/*.entitlements"
      - path: Shared
    configFiles:
      Debug: Secrets.xcconfig
      Release: Secrets.xcconfig
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lukashellesch.lockin
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_FILE: LockIn/Info.plist
        CODE_SIGN_ENTITLEMENTS: LockIn/LockIn.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    entitlements:
      path: LockIn/LockIn.entitlements
      properties:
        com.apple.developer.applesignin:
          - Default
        com.apple.developer.family-controls: true
        aps-environment: development
        com.apple.security.application-groups:
          - group.com.lukashellesch.lockin
    dependencies:
      - target: ShieldConfigurationExtension
      - target: ShieldActionExtension
      - target: DeviceActivityMonitorExtension
      - package: Supabase

  # ─── Shield Configuration Extension ────────────────────
  ShieldConfigurationExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: ShieldConfiguration
        excludes:
          - "**/*.entitlements"
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lukashellesch.lockin.ShieldConfiguration
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_FILE: ShieldConfiguration/Info.plist
        CODE_SIGN_ENTITLEMENTS: ShieldConfiguration/ShieldConfiguration.entitlements
    entitlements:
      path: ShieldConfiguration/ShieldConfiguration.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.lukashellesch.lockin

  # ─── Shield Action Extension ───────────────────────────
  ShieldActionExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: ShieldAction
        excludes:
          - "**/*.entitlements"
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lukashellesch.lockin.ShieldAction
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_FILE: ShieldAction/Info.plist
        CODE_SIGN_ENTITLEMENTS: ShieldAction/ShieldAction.entitlements
    entitlements:
      path: ShieldAction/ShieldAction.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.lukashellesch.lockin

  # ─── Device Activity Monitor Extension ─────────────────
  DeviceActivityMonitorExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: DeviceActivityMonitor
        excludes:
          - "**/*.entitlements"
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lukashellesch.lockin.DeviceActivityMonitor
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_FILE: DeviceActivityMonitor/Info.plist
        CODE_SIGN_ENTITLEMENTS: DeviceActivityMonitor/DeviceActivityMonitor.entitlements
    entitlements:
      path: DeviceActivityMonitor/DeviceActivityMonitor.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.lukashellesch.lockin
